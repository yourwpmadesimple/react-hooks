import React, { useReducer } from "react";

function App() {

  return (
    <div>
      <h1 className="center">const [state, dispatch] = useReducer(reducer, initialState, init)</h1>
    </div>
  );
}

export default App;
